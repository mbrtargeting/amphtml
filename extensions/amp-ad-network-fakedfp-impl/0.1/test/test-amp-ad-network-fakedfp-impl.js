import '../../../amp-ad/0.1/amp-ad';

// eslint-disable-next-line no-unused-vars
describes.fakeWin('amp-ad-network-fakedfp-impl', {amp: true}, env => {
  // beforeEach(() => {
  // });

  describe('#getAdUrl', () => {
    it('should do something', () => {
      expect('abc').to.equal('abc');
    });
  });
});
